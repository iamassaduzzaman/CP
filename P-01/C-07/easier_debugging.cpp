#include <bits/stdc++.h>
using namespace std;

#define trace(x) cerr << #x << ": " << x << " " << '\n';

int main(){

    int x = 10, y = 40, z = 50;
    trace(x);
    trace(y);
    trace(z);
    return 0;
}