#include <bits/stdc++.h>
using namespace std;

const int N = 1e6; 

bitset<N> bs;
vector<int> v(N);
vector<bool> v(N);
bool a[N];

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    
    return 0;
}